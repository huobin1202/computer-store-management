<Page x:Class="ComputerStoreManagement.Views.ProductsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Products">

    <Grid Background="{StaticResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Style="{StaticResource CardStyle}">
            <Grid>
                <TextBlock Text="Quản lý Sản phẩm" 
                          Style="{StaticResource TitleTextStyle}"
                          VerticalAlignment="Center"/>
                
                <TextBox materialDesign:HintAssist.Hint="Tìm kiếm..."
                        Style="{StaticResource MaterialDesignOutlinedTextBox}"
                        Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                        Width="300"
                        HorizontalAlignment="Right"/>
            </Grid>
        </Border>

        <!-- Products DataGrid -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}">
            <DataGrid ItemsSource="{Binding Products}"
                     SelectedItem="{Binding SelectedProduct}"
                     AutoGenerateColumns="False"
                     CanUserAddRows="False"
                     IsReadOnly="True"
                     SelectionMode="Single"
                     HeadersVisibility="All">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Mã SP" Binding="{Binding ProductCode}" Width="100"/>
                    <DataGridTextColumn Header="Tên sản phẩm" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="Danh mục" Binding="{Binding Category.Name}" Width="120"/>
                    <DataGridTextColumn Header="Thương hiệu" Binding="{Binding Brand.Name}" Width="120"/>
                    <DataGridTextColumn Header="Giá bán" Binding="{Binding SellPrice, Converter={StaticResource CurrencyConverter}}" Width="120"/>
                    <DataGridTextColumn Header="Số lượng" Binding="{Binding Quantity}" Width="80"/>
                    <DataGridTextColumn Header="Trạng thái" Binding="{Binding Status}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Page>
